# scheduling/results/ — simulation runs

**Step 4 (Part A) · Lead: Karl (with James)**

One CSV per simulation run, each recording the policy parameters (threshold, workload
profile, grid window) alongside the outputs (clean-energy gain, deferred fraction).
Committed to git — small and redistributable.

Regenerate rather than hand-edit, so every number traces to a run and its parameters.

**Done** when runs across a range of scheduling policies are committed here, enough to
state whether renewable-following scheduling is worth it for NZ.
